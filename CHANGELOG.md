# Changelog

本项目的公开版本变更记录维护在此文件中。

## [1.0.1] - 2026-08-26

修复定时执行从后台打开微信独立搜索框后，搜索框未获得键盘焦点，导致联系人和消息按键虽然已投递却没有被微信接收的问题。

### 修复

- 通过 Accessibility API 定位微信的 `AXSearchField`，不再盲目向无焦点搜索框发送粘贴快捷键。
- 使用 `AXValue` 写入联系人并回读确认；写入不一致时会在选择联系人前安全停止。
- 使用搜索框原生 `AXConfirm` 动作选择第一项，避免依赖不稳定的键盘焦点。
- 定时执行与“立即执行”统一使用相同的窗口恢复路径。

### 日志

- 执行记录区分“定时执行”“立即执行”和“安全验收”。
- 增加搜索框就绪、联系人值确认、首项确认和最终发送 Enter 投递标记。
- “已提交发送”文案改为“已向微信投递发送按键”，避免被理解为送达回执。

## [1.0.0] - 2026-08-26

首个公开版本。

### 功能

- 创建单次、每天和每周微信发送任务。
- 支持文字和多个本机附件。
- 提供默认安全预览与可显式开启的真实发送模式。
- 支持菜单栏入口、执行记录和 LaunchAgent 登录常驻。
- 显示微信运行状态、本机账号标识和自定义账号名称。

### 安全性

- 联系人唯一性改为任务级强制确认。
- 使用微信“编辑 → 搜索”后直接粘贴名称并按 Enter，不截图、不做 OCR。
- 过期超过两分钟的任务不会静默补发。
- 不读取或解密微信数据库，不需要微信数据库密钥。

### 稳定性

- 手动执行结束后恢复“微信发送”窗口。
- 安装更新前先卸载 KeepAlive LaunchAgent，避免旧进程在替换期间重新启动。
- 应用使用稳定的 Bundle ID 与指定要求签名，减少覆盖更新后重复授权。
