# Changelog

本项目的公开版本变更记录维护在此文件中。

## [1.0.4] - 2026-08-26

修复微信搜索浮窗未取得焦点时，联系人和消息可能被投递到当前聊天窗口的问题。

### 修复

- 优先选择微信主进程，不再误选 `WeChatAppEx` 辅助进程。
- 定时执行改用主线程 Launch Services 激活微信，并提升主窗口，修复激活调用返回成功但前台仍停留在“微信发送”导致菜单不可用的问题。
- 状态栏检查与执行流程统一使用微信主进程和主线程菜单树，减少后台状态误报。
- 通过 WindowServer 元数据确认微信独立搜索浮窗已经出现。
- 在浮窗顶部搜索框执行一次点击后再输入联系人，并恢复鼠标原位置。
- 按 Enter 后确认搜索浮窗已关闭；未关闭时停止，不会粘贴消息或发送。
- 增加 `search_panel_found`、`search_field_clicked` 和 `search_panel_closed` 阶段日志。

### 安全性

- 只读取窗口 PID、层级和边界，不读取窗口图像、文本或微信数据。
- 不恢复截图、OCR 或屏幕录制权限依赖。

## [1.0.3] - 2026-08-26

移除不稳定的可见截图/OCR 链路，修复“立即执行”覆盖定时任务状态而导致到点不触发的问题，并减少 macOS 隐私授权。

### 修复

- 搜索联系人后等待结果并直接按 Enter，不再触发微信截图层或依赖 OCR 判定。
- “立即执行”只写执行日志，不再修改任务的计划时间、启用状态或 `pending` 状态。
- 定时器加入主运行循环 `.common` 模式，应用在后台时仍持续检查到期任务。
- 执行前记录当前应用，结束后恢复原应用；不再主动隐藏“微信发送”窗口。

### 权限与隐私

- 移除 `ScreenCaptureKit`、Vision OCR 和屏幕录制权限申请。
- 停止读取微信 `xwechat_files/all_users/login` 目录，避免触发“访问其他 App 数据”授权。
- 微信状态改为通过 `NSWorkspace` 和 Accessibility 菜单可用性检查；账号名称由用户自行标注。
- 应用现在只需要一次辅助功能授权。

### 测试与日志

- 增加立即执行不消耗定时计划、定时成功推进单次任务的状态回归测试。
- 更新执行阶段日志，区分搜索输入、等待结果、用户唯一性确认和 Enter 投递。

## [1.0.2] - 2026-08-26

恢复搜索结果截图验证，并针对微信 4.1.80 中搜索控件不稳定暴露、直接窗口截图为黑屏的问题改用微信自身截图层。

### 修复

- 联系人改为通过 `CGEvent` 逐字 Unicode 输入，确保微信真正刷新搜索结果；不再依赖仅改变显示值的粘贴或 `AXValue`。
- 触发微信 `Control-Command-K` 全局截图工具，再用 `ScreenCaptureKit` 从合成显示画面裁剪搜索面板。
- 截图完成后自动右键退出微信截图层，并恢复执行前的鼠标位置。
- 搜索控件不出现在 Accessibility 树中时仍可验证和执行。

### 安全性

- 使用 Apple Vision 在本机内存中分别核对搜索框、结果分区和联系人名称。
- 仅统计“联系人”“群聊”“功能”“公众号”等可聊天分区，排除网络建议与聊天记录。
- 仅有一个精确匹配时才按 Enter；零结果、同名、OCR 失败、截图被遮挡或截图层未退出时均安全停止。
- 增加屏幕录制权限状态和用途说明；截图不保存、不上传。

### 测试与日志

- 增加唯一结果、同名拒绝、搜索框不一致和分区排除回归测试。
- 增加联系人逐字输入、截图层请求与关闭、OCR 回读、唯一结果和搜索 Enter 阶段标记。

## [1.0.1] - 2026-08-26

修复定时执行从后台打开微信独立搜索框后，搜索框未获得键盘焦点，导致联系人和消息按键虽然已投递却没有被微信接收的问题。

### 修复

- 通过 Accessibility API 定位微信的 `AXSearchField`，不再盲目向无焦点搜索框发送粘贴快捷键。
- 使用 `AXValue` 写入联系人并回读确认；写入不一致时会在选择联系人前安全停止。
- 使用搜索框原生 `AXConfirm` 动作选择第一项，避免依赖不稳定的键盘焦点。
- 定时执行与“立即执行”统一使用相同的窗口恢复路径。

### 日志

- 执行记录区分“定时执行”“立即执行”和“安全验收”。
- 增加搜索框就绪、联系人值确认、首项确认和最终发送 Enter 投递标记。
- “已提交发送”文案改为“已向微信投递发送按键”，避免被理解为送达回执。

## [1.0.0] - 2026-08-26

首个公开版本。

### 功能

- 创建单次、每天和每周微信发送任务。
- 支持文字和多个本机附件。
- 提供默认安全预览与可显式开启的真实发送模式。
- 支持菜单栏入口、执行记录和 LaunchAgent 登录常驻。
- 显示微信运行状态、本机账号标识和自定义账号名称。

### 安全性

- 联系人唯一性改为任务级强制确认。
- 使用微信“编辑 → 搜索”后直接粘贴名称并按 Enter，不截图、不做 OCR。
- 过期超过两分钟的任务不会静默补发。
- 不读取或解密微信数据库，不需要微信数据库密钥。

### 稳定性

- 手动执行结束后恢复“微信发送”窗口。
- 安装更新前先卸载 KeepAlive LaunchAgent，避免旧进程在替换期间重新启动。
- 应用使用稳定的 Bundle ID 与指定要求签名，减少覆盖更新后重复授权。
